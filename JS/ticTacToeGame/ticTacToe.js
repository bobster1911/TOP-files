// the logic behind the state of the game board.
const gameBoard = (() => {
    const create = () => {
        // create blank array
        const LEN = 9;
        const arr = new Array (LEN).fill(0);
        return arr;
    };
    return {
        create
    };
}) ();

const displayBoard = (() => {
    const create = () => {
        document.add
    }
}) ();